# Claude 司令塔（Zenn 記事リポジトリ）

このリポジトリは IVRy の Zenn 記事ソース（`articles/*.md`、`books/`、`images/`）を管理する。
Claude は毎ターン本ファイルを参照する。**短く保ち、詳細は Skill / 他ファイルに委譲する。**

---

## Inform（前提）

- **言語**: 日本語で返答する
- **対象媒体**: Zenn（IVRy publication）
- **記事の種類**: 技術解説・思考/洞察・課題解決
- **公開フロー**: GitHub の登録ブランチに push されると Zenn が自動デプロイ。`articles/*.md` の `published: true` で公開状態

## ディレクトリ構成

| パス | 用途 |
|---|---|
| `articles/` | Zenn の記事本体（Markdown） |
| `books/` | Zenn Book（章立て記事） |
| `images/` | 記事に貼る画像。記事スラッグごとにサブディレクトリを切る |
| `.claude/skills/blog-writing/` | 通常の記事執筆スキル |
| `.claude/skills/viral-blog/` | PV / 拡散を狙うときの執筆スキル |
| `zenn-cli-quick-reference.md` | Zenn CLI の使い方リファレンス |

## タスク自動判定

依頼内容から下記を判定し、該当する Skill を **暗黙に** 起動する。

| 判定 | キーワード例 | 使うリソース |
|---|---|---|
| 記事執筆（通常） | 「記事書いて」「ブログ書いて」「ドラフト」「Zenn 記事」 | Skill: `blog-writing` |
| 記事執筆（PV重視） | 「バズる記事」「PV を稼ぐ」「SNS で拡散」 | Skill: `viral-blog` |
| Zenn CLI 操作 | 「新しい記事作って」「プレビュー」「公開予約」 | `zenn-cli-quick-reference.md` |
| PR 作成 | 「PR 上げて」「プルリク作って」「公開したい」 | 本ファイル「PR 作成時の必須チェック」を実行 |

複数該当する場合は両方読む（例：執筆＋PR作成）。

---

## Constrain（やってはいけないこと）

- 顧客・取引先の固有名詞、業種、規模、案件名をそのまま書かない（必ず匿名化）
- 同僚・他社員の実名は本人公開資料の引用に留める。社内のみで知りうる情報は載せない
- 社内システム名・テーブル名・スキーマ名・本番URLを記事に書かない
- 出典のないデータ・引用を載せない
- コミットメッセージに `Co-Authored-By` 行を含めない
- 「いかがでしたか？」「本記事では〜について解説します」等の AI 生成感のある定型文を使わない

---

## PR 作成時の必須チェック

**push 後に必ず以下を全項目実行し、結果をユーザーに報告する。**
チェック未実施で「PR 作成完了」と報告しない。

### 1. 文章チェック（writing.md ルールに照合）

- 文体（です・ます調 / である調）がファイル内で統一されているか
- TL;DR が冒頭にあるか。ない場合は追加を提案する
- 見出し構成が「背景 → 本題 → 失敗談 / ハマりどころ → まとめ」の流れになっているか
- 「いかがでしたか？」「本記事では〜について解説します」等の AI 生成感のある定型文が無いか
- 一文が長すぎないか（目安60字）。「〜と思います」「〜かもしれません」が多用されていないか
- 太字記法 `**…**` の中に句点 `。` が入って見た目が崩れていないか
- 体言止めの連続でリズムが死んでいないか
- TL;DR や箇条書きの順序に論理破綻がないか（抽象→具体、または時系列で揃っているか）

### 2. コードブロック・図・リンクのチェック

- コードブロックに言語指定があるか（` ```python `、` ```sql ` 等）
- 図にキャプションと出典が付いているか
- 本文中の外部リンクをすべて死活確認する：
  ```bash
  for u in <URL...>; do
    code=$(curl -o /dev/null -s -w "%{http_code}" -L -A "Mozilla/5.0" --max-time 15 "$u")
    printf "%s %s\n" "$code" "$u"
  done
  ```
  403/404 が出たものは個別に判断（Medium 等は bot 検知で 403 を返すが正常）
- Zenn のリンクカード（行頭にURL単独）と、出典リンク（Markdown記法）の使い分けが意図通りか

### 3. 個人情報・機密情報の漏洩チェック

以下を本文中で grep / 目視確認する：

- 顧客・取引先の固有名詞、業種、規模、案件名
- 同僚・他社員の実名
- 社内システム名・テーブル名・スキーマ名・URLの内部ホスト
- 具体的な数値（売上・MRR・顧客数・SLA・件数など）→ 公開可否を確認
- 本番環境の構成・認証情報・トークンらしき文字列
- 第三者の発言を許可なく引用していないか

### 4. レピュテーションリスクチェック

- 顧客への責任転嫁になっていないか（「お客さんが要件を変えた」風の書き方は避ける）
- 過去/現在の所属企業・チーム・同僚への批判と取れる表現がないか
- 失敗の自己開示が、現職での重大インシデント告白になっていないか（前職の話として整理するか、抽象化する）
- 採用導線・宣伝が過剰になっていないか（HERP 等の公式リンクのみに留める）

### 5. Zenn フロントマター

- `title`: 検索される名詞を含むか（手段＋目的の形が望ましい）
- `emoji`: 1 文字のみ
- `type`: `tech` か `idea` のどちらが適切か
- `topics`: 5 つ以内に絞る
- `published`: PR で公開する場合は `true`、ドラフト保管なら `false`
- `publication_name`: IVRy publication に出す場合は `"ivry"`

---

## PR 作成手順（このリポジトリ専用）

- PR は **draft** で作成（`--draft`）
- ラベル `AI:100%` を付与
- PR 本文は短く：「なぜ / 何を」「変更点」「レビューしてほしい点」
- PR 説明にチェックリスト結果へのリンクは不要（PR コメントとしてユーザーへ直接報告する）

## チェック結果の報告フォーマット

PR 作成後、以下の形でユーザーに報告する：

```
## 記事チェック結果
- 対象: <ファイルパス>
- PR: <URL>

### 1. 文章面
（writing.md ルールに照らした所見。問題なしならその旨を明記）

### 2. 個人情報・企業情報の漏洩
| 項目 | 内容 | 判定 |
（テーブルで列挙）

### 3. レピュテーションリスク
（観点ごとに低/中/高で判定）

### 4. リンク死活
（HTTP ステータス一覧）

### 結論
（公開して問題ないか、修正推奨点があるか）
```
